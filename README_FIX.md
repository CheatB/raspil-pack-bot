# 🔧 Исправление бота - выполните ОДНУ из команд:

## Вариант 1: Node.js скрипт (рекомендуется)

```bash
cd /var/www/projects/emoji_bot
node fix.js
```

## Вариант 2: Bash скрипт

```bash
cd /var/www/projects/emoji_bot
chmod +x FIX_NOW.sh
./FIX_NOW.sh
```

## Вариант 3: Вручную (если скрипты не работают)

```bash
cd /var/www/projects/emoji_bot

# 1. Установить зависимости
npm install
cd apps/web && npm install && cd ../..

# 2. Пересобрать проект
NODE_OPTIONS='--max-old-space-size=1536' npm run build

# 3. Перезапустить PM2
pm2 delete emoji_bot_web
pm2 start ecosystem.config.js
pm2 save

# 4. Настроить webhook
npm run set:webhook

# 5. Проверить статус
pm2 list
pm2 logs emoji_bot_web --lines 20
```

## После выполнения:

1. Проверьте статус: `pm2 list` - должен быть `online`
2. Проверьте логи: `pm2 logs emoji_bot_web`
3. Отправьте команду боту и проверьте логи

Если всё работает - бот должен отвечать на команды!

