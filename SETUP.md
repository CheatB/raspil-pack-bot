# Инструкция по настройке и запуску

## Шаги для запуска проекта

### 1. ✅ Установка зависимостей
```bash
pnpm install
```
**Статус:** Выполнено

### 2. ✅ Создание .env.local
Файл `.env.local` создан. **Необходимо заполнить реальные значения:**

```env
TG_BOT_TOKEN=your_bot_token_here          # Получите у @BotFather в Telegram
WEBHOOK_SECRET=some-long-random-secret-change-this  # Сгенерируйте случайную строку
INTERNAL_KEY=internal-secret-key-change-this        # Сгенерируйте случайную строку
APP_BASE_URL=http://localhost:3000        # Для локальной разработки
NODE_ENV=development
DATABASE_URL="file:./dev.db"
```

**Важно:** Замените `your_bot_token_here` на реальный токен бота!

### 3. ✅ Генерация Prisma клиента и миграции
```bash
pnpm prisma:generate
pnpm prisma:migrate
```
**Статус:** Выполнено. База данных создана в `prisma/dev.db`

### 4. ✅ Проверка сборки
```bash
pnpm --filter processor build
pnpm --filter web typecheck
```
**Статус:** Проект компилируется

### 5. Запуск проекта и настройка webhook

#### 5.1. Запуск dev сервера
```bash
pnpm dev
```
Сервер запустится на `http://localhost:3000`

#### 5.2. Настройка туннеля (для локальной разработки)

**Вариант 1: ngrok**
```bash
# Установите ngrok: https://ngrok.com/
ngrok http 3000
# Скопируйте полученный URL (например: https://abc123.ngrok.io)
```

**Вариант 2: Cloudflared**
```bash
# Установите cloudflared
brew install cloudflared  # для macOS
cloudflared tunnel --url http://localhost:3000
# Скопируйте полученный URL
```

#### 5.3. Обновление APP_BASE_URL
Обновите `.env.local`:
```env
APP_BASE_URL=https://your-tunnel-url.ngrok.io  # или cloudflared URL
```

#### 5.4. Установка webhook
```bash
pnpm set:webhook
```

## Проверка работы

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Нажмите "🎨 Сгенерировать пак"
4. Отправьте изображение
5. Должно появиться превью мозаики

## Полезные команды

```bash
# Запуск dev сервера
pnpm dev

# Проверка типов
pnpm typecheck

# Линтинг
pnpm lint

# Форматирование кода
pnpm format

# Prisma Studio (просмотр БД)
pnpm --filter web db:studio

# Сброс webhook (если нужно)
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook"
```

## Решение проблем

### Ошибка: "Cannot find module"
- Убедитесь, что выполнили `pnpm install`
- Проверьте, что все workspace пакеты установлены

### Ошибка: "Webhook failed"
- Проверьте, что `APP_BASE_URL` указывает на публичный URL
- Убедитесь, что туннель (ngrok/cloudflared) запущен
- Проверьте `WEBHOOK_SECRET` в `.env.local`

### Ошибка: "Quota limit exceeded"
- Это нормально для Free тарифа (5 обработок/месяц)
- Проверьте базу данных через `pnpm --filter web db:studio`

## Следующие шаги

После успешного запуска:
1. Протестируйте все функции бота
2. Настройте деплой на Vercel
3. Настройте переменные окружения в Vercel
4. Обновите webhook на production URL

